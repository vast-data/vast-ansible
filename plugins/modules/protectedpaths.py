#!/usr/bin/python
# -*- coding: utf-8 -*-
# Copyright: (c) 2026, VAST Data
# GNU General Public License v3.0+ (see COPYING or https://www.gnu.org/licenses/gpl-3.0.txt)

from __future__ import absolute_import, division, print_function

__metaclass__ = type

# BEGIN AUTOGENERATED DOCUMENTATION
DOCUMENTATION = r"""
---
module: protectedpaths
short_description: Create, update, or delete VAST ProtectedPath resources
description:
  - Create, update, or delete VAST ProtectedPath resources.
  - Supports check_mode and diff for idempotent operations.
version_added: "1.0.0"
options:
  vms:
    description: VAST VMS connection parameters.
    type: dict
    required: true
    suboptions:
      host:
        description: VAST VMS hostname or IP address.
        required: true
        type: str
      validate_certs:
        description: Validate SSL certificates.
        type: bool
        default: true
      timeout:
        description: Request timeout in seconds.
        type: int
      token:
        description: API token (VAST 5.3+). Mutually exclusive with username/password.
        type: str
      username:
        description: Username for authentication. Mutually exclusive with token.
        type: str
      password:
        description: Password for authentication. Mutually exclusive with token.
        type: str
      tenant:
        description: Tenant name (optional).
        type: str
      api_version:
        description: API version (optional). Defaults to 'latest' if not specified.
        type: str
      debug:
        description: Enable HTTP debug traces. Traces are emitted as warnings on failure.
        type: bool
        default: false
  id:
    description: Resource ID for direct lookup. Mutually exclusive with name-based identification.
    type: int
  abort:
    description: "abort the failover process"
    type: bool

  capabilities:
    description: |
      Indicates if the protected path supports global access streams (\"STARED_GLOBAL_NAMESPACE\") or async replication
      streams (\"ASYNC_REPLICATION\") or a single stream for synchronous replication (\"SYNC_REPLICATION\") or both
      global access and async replication (\"REPLICATION_AND_GN\").
    type: str

  enabled:
    description: "enable/pause protected path"
    type: bool

  failover:
    description: "Trigger failover command"
    type: bool

  graceful:
    description: "graceful failover"
    type: bool

  lease_expiry_time:
    description: |
      The lease expiry time, in seconds, for a global access protected path. This is the duration for which data that
      was already requested at the destination path can be read locally from cache without the destination peer
      requesting it from the source peer. When the lease expires, the cache is invalidated and the next read request
      for the data is requested again from the source peer.
    type: int

  name:
    description: "Name"
    type: str

  protection_policy_id:
    description: "protection policy id"
    type: str

  remote_target_id:
    description: "Remote target ID for streams with global namespace or synchronous replication capability."
    type: int

  remote_tenant_guid:
    description: "Tenant GUID of the remote peer tenant to which to replicate"
    type: str

  replication_policy_id:
    description: "replication policy id"
    type: str

  source_dir:
    description: "path to replicate"
    type: str

  source_member_capabilities:
    description: "Stream capabilities for the source member"
    type: str

  sync_disconnect_time:
    description: |
      A period of time, in seconds, without communication between sync replication peers, after which the peers are
      disconnected.\"
    type: int

  sync_interval:
    description: |
      Minimal duration since the last snapshot shared between all destination peers in a replication group. Applicable
      if the protected path has more than one replication stream.
    type: int

  target_exported_dir:
    description: "where to replicate on the remote"
    type: str

  tenant_id:
    description: "Tenant ID of the tenant on the local cluster to which the source_dir belongs."
    type: int
  state:
    description: Desired state of the resource.
    type: str
    choices: [present, absent]
    default: present
  wait:
    description: Wait for async operations to complete.
    type: bool
    default: true
  wait_timeout:
    description: Timeout in seconds for async operations.
    type: int
    default: 300
author:
  - VAST Data (@vastdata)
extends_documentation_fragment:
  - ansible.builtin.action_common_attributes
attributes:
  check_mode:
    support: full
  diff_mode:
    support: full
  platform:
    platforms: posix
"""
# END AUTOGENERATED DOCUMENTATION

# BEGIN AUTOGENERATED EXAMPLES
EXAMPLES = r"""
- name: Create ProtectedPath
  vastdata.vms.protectedpaths:
    vms:
      host: vast-vms.example.com
      token: "{{ vast_token }}"
    name: "example-name"
    source_dir: "example-source_dir"
    target_exported_dir: "example-target_exported_dir"
    tenant_id: 1
    state: present

- name: Update ProtectedPath
  vastdata.vms.protectedpaths:
    vms:
      host: vast-vms.example.com
      token: "{{ vast_token }}"
    name: "example-value"
    state: present

- name: Delete ProtectedPath
  vastdata.vms.protectedpaths:
    vms:
      host: vast-vms.example.com
      token: "{{ vast_token }}"
    name: "example-value"
    state: absent
"""
# END AUTOGENERATED EXAMPLES

# BEGIN AUTOGENERATED RETURN
RETURN = r"""
changed:
  description: Whether a change was made.
  type: bool
  returned: always
protectedpaths:
  description: The protectedpaths resource data.
  type: dict
  returned: always
  sample:
    id: 1
    name: "example"
diff:
  description: Before/after diff when changed.
  type: dict
  returned: when changed
"""
# END AUTOGENERATED RETURN

# BEGIN AUTOGENERATED IMPORTS
from typing import Any, Dict

from ansible.module_utils.basic import AnsibleModule
from ansible_collections.vastdata.vms.plugins.module_utils.vast.errors import VastError
from ansible_collections.vastdata.vms.plugins.module_utils.vast.resource import BaseResource

# END AUTOGENERATED IMPORTS

# BEGIN AUTOGENERATED ARGUMENT_SPEC
ARGUMENT_SPEC: Dict[str, Any] = {
    "vms": {
        "type": "dict",
        "required": True,
        "options": {
            "host": {"required": True, "type": "str"},
            "validate_certs": {"type": "bool", "default": True},
            "timeout": {"type": "int", "default": None},
            "token": {"type": "str", "default": None, "no_log": True},
            "username": {"type": "str", "default": None},
            "password": {"type": "str", "default": None, "no_log": True},
            "tenant": {"type": "str", "default": None},
            "api_version": {"type": "str", "default": None},
            "debug": {"type": "bool", "default": False},
        },
        "mutually_exclusive": [
            ("token", "username"),
            ("token", "password"),
        ],
    },
    "id": {"type": "int", "default": None},
    "abort": {"type": "bool", "default": None},
    "capabilities": {"type": "str", "default": None},
    "enabled": {"type": "bool", "default": None},
    "failover": {"type": "bool", "default": None},
    "graceful": {"type": "bool", "default": None},
    "lease_expiry_time": {"type": "int", "default": None},
    "name": {"type": "str", "default": None},
    "protection_policy_id": {"type": "str", "default": None},
    "remote_target_id": {"type": "int", "default": None},
    "remote_tenant_guid": {"type": "str", "default": None},
    "replication_policy_id": {"type": "str", "default": None},
    "source_dir": {"type": "str", "default": None},
    "source_member_capabilities": {"type": "str", "default": None},
    "sync_disconnect_time": {"type": "int", "default": None},
    "sync_interval": {"type": "int", "default": None},
    "target_exported_dir": {"type": "str", "default": None},
    "tenant_id": {"type": "int", "default": None},
    "state": {"type": "str", "choices": ["present", "absent"], "default": "present"},
    "wait": {"type": "bool", "default": True},
    "wait_timeout": {"type": "int", "default": 300},
}
# END AUTOGENERATED ARGUMENT_SPEC

# BEGIN AUTOGENERATED RESOURCE_CLASS


class ProtectedpathResource(BaseResource):
    """Resource manager for ProtectedPath."""

    resource_name = "protectedpaths"
    singular = "protectedpath"
    lookup_field = "name"
    async_delete = True
    create_only_fields = {
        "capabilities",
        "remote_target_id",
        "remote_tenant_guid",
        "source_member_capabilities",
        "tenant_id",
    }  # Fields only valid during creation
    update_only_fields = {"abort", "failover", "graceful", "replication_policy_id", "state"}  # Fields only valid during updates


# END AUTOGENERATED RESOURCE_CLASS

# BEGIN AUTOGENERATED MAIN


def main() -> None:
    """Module entry point."""
    module = AnsibleModule(
        argument_spec=ARGUMENT_SPEC,
        supports_check_mode=True,
    )

    try:
        resource = ProtectedpathResource(module)
        resource.run()
    except VastError as e:
        module.fail_json(msg=str(e))


if __name__ == "__main__":
    main()
# END AUTOGENERATED MAIN
