#!/usr/bin/python
# -*- coding: utf-8 -*-
# Copyright: (c) 2026, VAST Data
# Apache License 2.0 (see LICENSE or https://www.apache.org/licenses/LICENSE-2.0)
# SPDX-License-Identifier: Apache-2.0

from __future__ import absolute_import, division, print_function

__metaclass__ = type

# BEGIN AUTOGENERATED DOCUMENTATION
DOCUMENTATION = r"""
---
module: ldaps_info
short_description: List VAST Ldap resources
description:
  - List VAST Ldap resources.
  - Read-only; always runs in check_mode and never reports changed.
  - Optional parameters are passed to the API as server-side query filters.
version_added: "1.0.0"
options:
  abac_read_only_value_name:
    description: "Filter by abac read only value name (optional filter)"
    type: str

  abac_read_write_value_name:
    description: "Filter by abac read write value name (optional filter)"
    type: str

  active_directory:
    description: "Filter by active directory (optional filter)"
    type: str

  active_directory_id:
    description: "Filter by active directory id (optional filter)"
    type: int

  advanced_filter:
    description: |
      Manual filters for the BaseDN. This is useful when accounts are distributed across OUs and the baseDN needs to be
      wide to include all accounts, while there are also accounts that you would like to exclude from user queries.
      (optional filter)
    type: str

  binddn:
    description: "Distinguished name of LDAP superuser (optional filter)"
    type: str

  bindpw:
    description: "Password for the LDAP superuser (optional filter)"
    type: str

  domain_name:
    description: "FQDN of the domain. (optional filter)"
    type: str

  gid_number:
    description: "Filter by gid number (optional filter)"
    type: str

  group_login_name:
    description: |
      The attribute used to query Active Directory for the group login name in NFS ID mapping. Applicable only with
      Active Directory and NFSv4. (optional filter)
    type: str

  group_searchbase:
    description: |
      Base DN for group queries within the joined domain only. When auto discovery is enabled, group queries outside
      the joined domain use auto-discovered Base DNs. (optional filter)
    type: str

  guid:
    description: "Filter by guid (optional filter)"
    type: str

  id:
    description: "Filter by id (optional filter)"
    type: int

  is_abac_provider:
    description: "is this Ldap / Active Directory used for ABAC (optional filter)"
    type: bool

  is_vms_auth_provider:
    description: |
      Enables use of the LDAP for VMS authentication. Two LDAP configurations per cluster can be used for VMS
      authentication: one with Active Directory and one without. (optional filter)
    type: bool

  mail_property_name:
    description: "The attribute to use for the user's email address. (optional filter)"
    type: str

  match_user:
    description: "Filter by match user (optional filter)"
    type: str

  method:
    description: "Bind Authentication Method (optional filter)"
    type: str

  monitor_action:
    description: |
      The type of periodic health check that VAST Cluster performs for the LDAP provider. PING (default, less overhead
      and impact on the provider) = pings the provider. BIND = binds to the provider. (optional filter)
    type: str
    choices: ["PING", "BIND"]

  name:
    description: "Name of the LDAP configuration (optional filter)"
    type: str

  netgroup_searchbase:
    description: "Base DN for netgroup queries. (optional filter)"
    type: str

  port:
    description: "LDAP server port. 389 (LDAP)  636 (LDAPS) (optional filter)"
    type: int

  posix_account:
    description: "Filter by posix account (optional filter)"
    type: str

  posix_attributes_source:
    description: "Defines which domains POSIX attributes will be supported from. (optional filter)"
    type: str
    choices: ["JOINED_DOMAIN", "ALL_DOMAINS", "SPECIFIC_DOMAINS", "GC"]

  posix_group:
    description: "Filter by posix group (optional filter)"
    type: str

  posix_primary_provider:
    description: "POSIX primary provider (optional filter)"
    type: bool

  query_groups_mode:
    description: "Query group mode (optional filter)"
    type: str

  query_posix_attributes_from_gc:
    description: |
      When set to True - users/groups from non-joined domain POSIX attributes are supported, when set to False - Posix
      attributes of users/groups from non-joined domain are not supported. As a condition Global catalog needs to be
      configured to support Posix attributes. (deprecated since 4.6) (optional filter)
    type: bool

  reverse_lookup:
    description: "Resolve LDAP netgroups into hostnames (optional filter)"
    type: bool

  searchbase:
    description: |
      The Base DN is the starting point the LDAP provider uses when searching for users and groups. If the Group Base
      DN is configured it will be used instead of the Base DN, for groups only (optional filter)
    type: str

  state:
    description: "Filter by state (optional filter)"
    type: str
    choices: ["CONNECTED", "DEGRADED", "DISCONNECTED", "UNKNOWN", "CONNECTING", "IN_DISCOVERY", "NOT_FOUND"]

  tenant_id:
    description: "Tenant ID (optional filter)"
    type: int

  title:
    description: "Filter by title (optional filter)"
    type: str

  tls_certificate:
    description: "Filter by tls certificate (optional filter)"
    type: str

  uid:
    description: "Filter by uid (optional filter)"
    type: str

  uid_member:
    description: "Filter by uid member (optional filter)"
    type: str

  uid_member_value_property_name:
    description: "The attribute which represents the value of the LDAP group's member property. (optional filter)"
    type: str

  uid_number:
    description: "Filter by uid number (optional filter)"
    type: str

  url:
    description: |
      Comma-separated list of URIs of LDAP servers (Domain Controllers (DCs) in Active Directory), in priority order.
      The URI with highest priority that has a good health status is used. Specify each URI in the format
      SCHEME://ADDRESS. ADDRESS can be either a DNS name or an IP address. e.g. ldap://ldap.company.com,
      ldaps://ldaps.company.com, ldap://192.0.2.2 (optional filter)
    type: str

  use_auto_discovery:
    description: |
      When enabled, Active Directory Domain Controllers (DCs) and Active Directory domains are auto discovered. Queries
      extend beyond the joined domain to all domains in the forest. When disabled, queries are restricted to the joined
      domain and DCs must be provided in the URLs field. (optional filter)
    type: bool

  use_ldaps:
    description: "Use LDAPS for auto-Discovery. To activate, set use-auto-discovery to true also. (optional filter)"
    type: bool

  use_multi_forest:
    description: "Allow access for users from trusted domains on other forests. (optional filter)"
    type: bool

  use_posix:
    description: "POSIX support (optional filter)"
    type: bool

  use_tls:
    description: "configure LDAP with TLS (optional filter)"
    type: bool

  user_login_name:
    description: |
      The attribute used to query Active Directory for the user login name in NFS ID mapping. Applicable only with
      Active Directory and NFSv4. (optional filter)
    type: str

  username_property_name:
    description: "Username property name (optional filter)"
    type: str
author:
  - VAST Data (@vastdata)
extends_documentation_fragment:
  - vastdata.vms.conn
  - ansible.builtin.action_common_attributes
attributes:
  check_mode:
    support: full
  diff_mode:
    support: N/A
  platform:
    platforms: posix
"""
# END AUTOGENERATED DOCUMENTATION

# BEGIN AUTOGENERATED EXAMPLES
EXAMPLES = r"""
- name: List all ldaps
  vastdata.vms.ldaps_info:
    vms:
      host: vast-vms.example.com
      token: "{{ vast_token }}"
  register: ldaps_result

- name: List ldaps filtered by name
  vastdata.vms.ldaps_info:
    vms:
      host: vast-vms.example.com
      token: "{{ vast_token }}"
    name: example-value
  register: filtered_ldaps
"""
# END AUTOGENERATED EXAMPLES

# BEGIN AUTOGENERATED RETURN
RETURN = r"""
changed:
  description: Always false; info modules never change state.
  type: bool
  returned: always
ldaps:
  description: List of matching ldaps resources.
  type: list
  elements: dict
  returned: always
  sample: []
"""
# END AUTOGENERATED RETURN

# BEGIN AUTOGENERATED IMPORTS
from typing import Any, Dict

from ansible.module_utils.basic import AnsibleModule
from ansible_collections.vastdata.vms.plugins.module_utils.vast.errors import VastError
from ansible_collections.vastdata.vms.plugins.module_utils.vast.info_resource import BaseInfoResource

# END AUTOGENERATED IMPORTS

# BEGIN AUTOGENERATED ARGUMENT_SPEC
ARGUMENT_SPEC: Dict[str, Any] = {
    "vms": {
        "type": "dict",
        "required": True,
        "options": {
            "host": {"required": True, "type": "str"},
            "validate_certs": {"type": "bool", "default": True},
            "timeout": {"type": "int", "default": None},
            "token": {"type": "str", "default": None, "no_log": True},
            "username": {"type": "str", "default": None},
            "password": {"type": "str", "default": None, "no_log": True},
            "tenant": {"type": "str", "default": None},
            "api_version": {"type": "str", "default": None},
            "debug": {"type": "bool", "default": False},
        },
        "mutually_exclusive": [
            ("token", "username"),
            ("token", "password"),
        ],
    },
    "abac_read_only_value_name": {"type": "str", "default": None},
    "abac_read_write_value_name": {"type": "str", "default": None},
    "active_directory": {"type": "str", "default": None},
    "active_directory_id": {"type": "int", "default": None},
    "advanced_filter": {"type": "str", "default": None},
    "binddn": {"type": "str", "default": None},
    "bindpw": {"type": "str", "default": None, "no_log": True},
    "domain_name": {"type": "str", "default": None},
    "gid_number": {"type": "str", "default": None},
    "group_login_name": {"type": "str", "default": None},
    "group_searchbase": {"type": "str", "default": None},
    "guid": {"type": "str", "default": None},
    "id": {"type": "int", "default": None},
    "is_abac_provider": {"type": "bool", "default": None},
    "is_vms_auth_provider": {"type": "bool", "default": None},
    "mail_property_name": {"type": "str", "default": None},
    "match_user": {"type": "str", "default": None},
    "method": {"type": "str", "default": None},
    "monitor_action": {"type": "str", "default": None, "choices": ["PING", "BIND"]},
    "name": {"type": "str", "default": None},
    "netgroup_searchbase": {"type": "str", "default": None},
    "port": {"type": "int", "default": None},
    "posix_account": {"type": "str", "default": None},
    "posix_attributes_source": {
        "type": "str",
        "default": None,
        "choices": ["JOINED_DOMAIN", "ALL_DOMAINS", "SPECIFIC_DOMAINS", "GC"],
    },
    "posix_group": {"type": "str", "default": None},
    "posix_primary_provider": {"type": "bool", "default": None},
    "query_groups_mode": {"type": "str", "default": None},
    "query_posix_attributes_from_gc": {"type": "bool", "default": None},
    "reverse_lookup": {"type": "bool", "default": None},
    "searchbase": {"type": "str", "default": None},
    "state": {
        "type": "str",
        "default": None,
        "choices": ["CONNECTED", "DEGRADED", "DISCONNECTED", "UNKNOWN", "CONNECTING", "IN_DISCOVERY", "NOT_FOUND"],
    },
    "tenant_id": {"type": "int", "default": None},
    "title": {"type": "str", "default": None},
    "tls_certificate": {"type": "str", "default": None},
    "uid": {"type": "str", "default": None},
    "uid_member": {"type": "str", "default": None},
    "uid_member_value_property_name": {"type": "str", "default": None},
    "uid_number": {"type": "str", "default": None},
    "url": {"type": "str", "default": None},
    "use_auto_discovery": {"type": "bool", "default": None},
    "use_ldaps": {"type": "bool", "default": None},
    "use_multi_forest": {"type": "bool", "default": None},
    "use_posix": {"type": "bool", "default": None},
    "use_tls": {"type": "bool", "default": None},
    "user_login_name": {"type": "str", "default": None},
    "username_property_name": {"type": "str", "default": None},
}
# END AUTOGENERATED ARGUMENT_SPEC

# BEGIN AUTOGENERATED RESOURCE_CLASS


class LdapInfoResource(BaseInfoResource):
    """Read-only list manager for Ldap."""

    resource_name = "ldaps"
    return_key = "ldaps"
    filter_fields = {
        "abac_read_only_value_name",
        "abac_read_write_value_name",
        "active_directory",
        "active_directory_id",
        "advanced_filter",
        "binddn",
        "bindpw",
        "domain_name",
        "gid_number",
        "group_login_name",
        "group_searchbase",
        "guid",
        "id",
        "is_abac_provider",
        "is_vms_auth_provider",
        "mail_property_name",
        "match_user",
        "method",
        "monitor_action",
        "name",
        "netgroup_searchbase",
        "port",
        "posix_account",
        "posix_attributes_source",
        "posix_group",
        "posix_primary_provider",
        "query_groups_mode",
        "query_posix_attributes_from_gc",
        "reverse_lookup",
        "searchbase",
        "state",
        "tenant_id",
        "title",
        "tls_certificate",
        "uid",
        "uid_member",
        "uid_member_value_property_name",
        "uid_number",
        "url",
        "use_auto_discovery",
        "use_ldaps",
        "use_multi_forest",
        "use_posix",
        "use_tls",
        "user_login_name",
        "username_property_name",
    }
    generated_min_version = (5, 4)  # oldest VMS version these modules were generated for
    field_versions = {
        "is_abac_provider": {"min": (5, 5), "max": None},
        "netgroup_searchbase": {"min": (5, 5), "max": None},
    }


# END AUTOGENERATED RESOURCE_CLASS

# BEGIN AUTOGENERATED MAIN


def main() -> None:
    """Module entry point."""
    module = AnsibleModule(
        argument_spec=ARGUMENT_SPEC,
        supports_check_mode=True,
    )

    try:
        resource = LdapInfoResource(module)
        resource.run()
    except VastError as e:
        module.fail_json(msg=str(e))


if __name__ == "__main__":
    main()
# END AUTOGENERATED MAIN
