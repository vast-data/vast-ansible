#!/usr/bin/python
# -*- coding: utf-8 -*-
# Copyright: (c) 2026, VAST Data
# Apache License 2.0 (see LICENSE or https://www.apache.org/licenses/LICENSE-2.0)
# SPDX-License-Identifier: Apache-2.0

from __future__ import absolute_import, division, print_function

__metaclass__ = type

# BEGIN AUTOGENERATED DOCUMENTATION
DOCUMENTATION = r"""
---
module: vippools_info
short_description: List VAST VIPPool resources
description:
  - List VAST VIPPool resources.
  - Read-only; always runs in check_mode and never reports changed.
  - Optional parameters are passed to the API as server-side query filters.
version_added: "1.0.0"
options:
  active_interfaces:
    description: "Number of active interfaces (optional filter)"
    type: int

  bgp_config_guid:
    description: "BGP Configuration GUID (optional filter)"
    type: str

  bgp_config_id:
    description: "The ID of the BGP configuration applied for L3 connectivity (optional filter)"
    type: int

  bgp_config_name:
    description: "BGP Configuration Name (optional filter)"
    type: str

  cluster:
    description: "Parent cluster (optional filter)"
    type: str

  cluster_id:
    description: "Cluster ID (optional filter)"
    type: int

  domain_name:
    description: |
      Domain name for the VAST DNS server. The domain suffix defined in the DNS server configuration is appended to
      this domain name to form a FQDN which the DNS server resolves to this VIP pool. (optional filter)
    type: str

  enable_l3:
    description: "When true, L3 networking is enabled. (optional filter)"
    type: bool

  enable_weighted_balancing:
    description: "Weighted Balancing Enabled (optional filter)"
    type: bool

  enabled:
    description: "True if the VIP pool is enabled (optional filter)"
    type: bool

  end_ip:
    description: "Not currently in use. Use ip_ranges instead. (optional filter)"
    type: str

  guid:
    description: "Global unique ID (optional filter)"
    type: str

  gw_ip:
    description: "The IP address of a local gateway device if client traffic is routed through one. (optional filter)"
    type: str

  gw_ipv6:
    description: "GW IPv6 Address (optional filter)"
    type: str

  id:
    description: "VIP Pool ID (optional filter)"
    type: int

  name:
    description: "VIP pool name (optional filter)"
    type: str

  peer_asn:
    description: "The client network's ASN. Applicable only if enable_l3 is true. (optional filter)"
    type: int

  port_membership:
    description: |
      Allocation of left, right or all CNode ports to the VIP pool. Allocating the left port and the right port in
      different VIP pools enables the CNodes to be connected to multiple networks simultaneously. Default: all
      (optional filter)
    type: str
    choices: ["RIGHT", "LEFT", "ALL"]

  ranges_summary:
    description: "IP ranges (optional filter)"
    type: str

  role:
    description: |
      'PROTOCOLS' dedicates the VIP pool to client traffic from all of the supported access protocols (NFSv3, NFSv4.1,
      NFSv4.2, SMBv2, S3, Database, Block, Kafka). At least one VIP pool must be created to enable client access.
      'REPLICATION' dedicates the VIP pool for connectivity with an async replication peer cluster. This is needed for
      async replication. 'BIG_CATALOG' dedicates the VIP pool to VAST Catalog query access from the client network.
      (optional filter)
    type: str
    choices: ["PROTOCOLS", "REPLICATION", "BIG_CATALOG", "QUERY_ENGINE_CNODE_GROUP"]

  start_ip:
    description: "Not currently in use. Use ip_ranges instead. (optional filter)"
    type: str

  state:
    description: "The state of the VIP pool (optional filter)"
    type: str

  subnet_cidr:
    description: |
      The subnet expressed as a CIDR index (number of bits in each IPv4 address that belong to the subnet) (optional
      filter)
    type: int

  subnet_cidr_ipv6:
    description: "IPv6 Subnet CIDR prefix (bits number) (optional filter)"
    type: int

  sync:
    description: "Synchronization state with leader (optional filter)"
    type: str

  sync_time:
    description: "Synchronization time with leader (optional filter)"
    type: str

  tenant_id:
    description: |
      Tenant ID. If set, only the tenant with this ID can access the VIP pool. If not set, the VIP pool is accessible
      to all tenants. (optional filter)
    type: int

  tenant_name:
    description: "Tenant Name (optional filter)"
    type: str

  title:
    description: "IP range of the VIP pool (optional filter)"
    type: str

  url:
    description: "The VIP Pool's endpoint URL for API operations (optional filter)"
    type: str

  vast_asn:
    description: "The cluster's ASN. Applicable only if enable_l3 is true. (optional filter)"
    type: int

  vip_allocation:
    description: "VIP allocation type (optional filter)"
    type: str

  vlan:
    description: |
      VIPPool VLAN, if the VIP pool is tagged with a specific VLAN. The VIP pool is exposed only to the specified VLAN
      on the client data network (optional filter)
    type: int

  vms_preferred:
    description: "If true, CNodes participating in the vip pool are preferred in VMS host election. (optional filter)"
    type: bool
author:
  - VAST Data (@vastdata)
extends_documentation_fragment:
  - vastdata.vms.conn
  - ansible.builtin.action_common_attributes
attributes:
  check_mode:
    support: full
  diff_mode:
    support: N/A
  platform:
    platforms: posix
"""
# END AUTOGENERATED DOCUMENTATION

# BEGIN AUTOGENERATED EXAMPLES
EXAMPLES = r"""
- name: List all vippools
  vastdata.vms.vippools_info:
    vms:
      host: vast-vms.example.com
      token: "{{ vast_token }}"
  register: vippools_result

- name: List vippools filtered by name
  vastdata.vms.vippools_info:
    vms:
      host: vast-vms.example.com
      token: "{{ vast_token }}"
    name: example-value
  register: filtered_vippools
"""
# END AUTOGENERATED EXAMPLES

# BEGIN AUTOGENERATED RETURN
RETURN = r"""
changed:
  description: Always false; info modules never change state.
  type: bool
  returned: always
vippools:
  description: List of matching vippools resources.
  type: list
  elements: dict
  returned: always
  sample: []
"""
# END AUTOGENERATED RETURN

# BEGIN AUTOGENERATED IMPORTS
from typing import Any, Dict

from ansible.module_utils.basic import AnsibleModule
from ansible_collections.vastdata.vms.plugins.module_utils.vast.errors import VastError
from ansible_collections.vastdata.vms.plugins.module_utils.vast.info_resource import BaseInfoResource

# END AUTOGENERATED IMPORTS

# BEGIN AUTOGENERATED ARGUMENT_SPEC
ARGUMENT_SPEC: Dict[str, Any] = {
    "vms": {
        "type": "dict",
        "required": True,
        "options": {
            "host": {"required": True, "type": "str"},
            "validate_certs": {"type": "bool", "default": True},
            "timeout": {"type": "int", "default": None},
            "token": {"type": "str", "default": None, "no_log": True},
            "username": {"type": "str", "default": None},
            "password": {"type": "str", "default": None, "no_log": True},
            "tenant": {"type": "str", "default": None},
            "api_version": {"type": "str", "default": None},
            "debug": {"type": "bool", "default": False},
        },
        "mutually_exclusive": [
            ("token", "username"),
            ("token", "password"),
        ],
    },
    "active_interfaces": {"type": "int", "default": None},
    "bgp_config_guid": {"type": "str", "default": None},
    "bgp_config_id": {"type": "int", "default": None},
    "bgp_config_name": {"type": "str", "default": None},
    "cluster": {"type": "str", "default": None},
    "cluster_id": {"type": "int", "default": None},
    "domain_name": {"type": "str", "default": None},
    "enable_l3": {"type": "bool", "default": None},
    "enable_weighted_balancing": {"type": "bool", "default": None},
    "enabled": {"type": "bool", "default": None},
    "end_ip": {"type": "str", "default": None},
    "guid": {"type": "str", "default": None},
    "gw_ip": {"type": "str", "default": None},
    "gw_ipv6": {"type": "str", "default": None},
    "id": {"type": "int", "default": None},
    "name": {"type": "str", "default": None},
    "peer_asn": {"type": "int", "default": None},
    "port_membership": {"type": "str", "default": None, "choices": ["RIGHT", "LEFT", "ALL"]},
    "ranges_summary": {"type": "str", "default": None},
    "role": {"type": "str", "default": None, "choices": ["PROTOCOLS", "REPLICATION", "BIG_CATALOG", "QUERY_ENGINE_CNODE_GROUP"]},
    "start_ip": {"type": "str", "default": None},
    "state": {"type": "str", "default": None},
    "subnet_cidr": {"type": "int", "default": None},
    "subnet_cidr_ipv6": {"type": "int", "default": None},
    "sync": {"type": "str", "default": None},
    "sync_time": {"type": "str", "default": None},
    "tenant_id": {"type": "int", "default": None},
    "tenant_name": {"type": "str", "default": None},
    "title": {"type": "str", "default": None},
    "url": {"type": "str", "default": None},
    "vast_asn": {"type": "int", "default": None},
    "vip_allocation": {"type": "str", "default": None},
    "vlan": {"type": "int", "default": None},
    "vms_preferred": {"type": "bool", "default": None},
}
# END AUTOGENERATED ARGUMENT_SPEC

# BEGIN AUTOGENERATED RESOURCE_CLASS


class VippoolInfoResource(BaseInfoResource):
    """Read-only list manager for VIPPool."""

    resource_name = "vippools"
    return_key = "vippools"
    filter_fields = {
        "active_interfaces",
        "bgp_config_guid",
        "bgp_config_id",
        "bgp_config_name",
        "cluster",
        "cluster_id",
        "domain_name",
        "enable_l3",
        "enable_weighted_balancing",
        "enabled",
        "end_ip",
        "guid",
        "gw_ip",
        "gw_ipv6",
        "id",
        "name",
        "peer_asn",
        "port_membership",
        "ranges_summary",
        "role",
        "start_ip",
        "state",
        "subnet_cidr",
        "subnet_cidr_ipv6",
        "sync",
        "sync_time",
        "tenant_id",
        "tenant_name",
        "title",
        "url",
        "vast_asn",
        "vip_allocation",
        "vlan",
        "vms_preferred",
    }
    generated_min_version = (5, 4)  # oldest VMS version these modules were generated for


# END AUTOGENERATED RESOURCE_CLASS

# BEGIN AUTOGENERATED MAIN


def main() -> None:
    """Module entry point."""
    module = AnsibleModule(
        argument_spec=ARGUMENT_SPEC,
        supports_check_mode=True,
    )

    try:
        resource = VippoolInfoResource(module)
        resource.run()
    except VastError as e:
        module.fail_json(msg=str(e))


if __name__ == "__main__":
    main()
# END AUTOGENERATED MAIN
