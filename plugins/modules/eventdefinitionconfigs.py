#!/usr/bin/python
# -*- coding: utf-8 -*-
# Copyright: (c) 2026, VAST Data
# GNU General Public License v3.0+ (see COPYING or https://www.gnu.org/licenses/gpl-3.0.txt)

from __future__ import absolute_import, division, print_function

__metaclass__ = type

# BEGIN AUTOGENERATED DOCUMENTATION
DOCUMENTATION = r"""
---
module: eventdefinitionconfigs
short_description: Update VAST EventDefinitionConfig resources
description:
  - Update VAST EventDefinitionConfig resources.
  - Supports check_mode and diff for idempotent operations.
version_added: "1.0.0"
options:
  vms:
    description: VAST VMS connection parameters.
    type: dict
    required: true
    suboptions:
      host:
        description: VAST VMS hostname or IP address.
        required: true
        type: str
      validate_certs:
        description: Validate SSL certificates.
        type: bool
        default: true
      timeout:
        description: Request timeout in seconds.
        type: int
      token:
        description: API token (VAST 5.3+). Mutually exclusive with username/password.
        type: str
      username:
        description: Username for authentication. Mutually exclusive with token.
        type: str
      password:
        description: Password for authentication. Mutually exclusive with token.
        type: str
      tenant:
        description: Tenant name (optional).
        type: str
      api_version:
        description: API version (optional). Defaults to 'latest' if not specified.
        type: str
      debug:
        description: Enable HTTP debug traces. Traces are emitted as warnings on failure.
        type: bool
        default: false
  id:
    description: Resource ID for direct lookup. Mutually exclusive with name-based identification.
    type: int
  audit_logs_retention:
    description: "Audit logs retention in days"
    type: int

  critical_value:
    description: "Maps CRITICAL severity to a different value. Default: CRITICAL"
    type: str
    choices: ["INFO", "MINOR", "MAJOR", "CRITICAL"]

  disable_actions:
    description: "Set to true to disable default actions for events."
    type: bool

  email_recipients:
    description: |
      Default email recipients. These recipients receive notifications of all alarms except those triggered by events
      that have a different list of email recipients specified in the event definition or for which actions are
      disabled.
    type: list
    elements: str

  email_sender:
    description: "Global for all alarm notification emails, the sender email that appears in the emails."
    type: str

  email_subject:
    description: |
      Optional and global email subject for all alarm notification emails. Leave blank to send alarm info in the
      subject.
    type: str

  enabled:
    description: "Enabled"
    type: bool

  info_value:
    description: "Maps INFO severity to a different severity value. Default: INFO"
    type: str
    choices: ["INFO", "MINOR", "MAJOR", "CRITICAL"]

  major_value:
    description: "Maps MAJOR severity to a different severity value. Default: MAJOR"
    type: str
    choices: ["INFO", "MINOR", "MAJOR", "CRITICAL"]

  minor_value:
    description: "Maps MINOR severity to a different severity value. Default: MINOR"
    type: str
    choices: ["INFO", "MINOR", "MAJOR", "CRITICAL"]

  quota_email_hourly_limit:
    description: "Maximum quota alert emails VMS will send per hour"
    type: int

  quota_email_interval:
    description: "The minimal interval time between quota alert emails sent to a user."
    type: str

  quota_email_provider:
    description: |
      Specify which query context should be used to query providers for user quota alert emails. 'Aggregated' will
      perform an aggregated query of all providers. Alternatively, you can specify a specific provider if connected to
      the cluster.
    type: str
    choices: ["aggregated", "ldap", "ad"]

  quota_email_suffix:
    description: |
      A default suffix to add to append to user names to form an email address. This is used as the email recipient
      address for sending a user user quota alert emails. It is only used if an email address is not found for the user
      on a provider.
    type: str

  smtp_host:
    description: "SMTP server host name for alert emails."
    type: str

  smtp_password:
    description: "Password for SMTP authentication"
    type: str

  smtp_port:
    description: "The port used by the SMTP server to send outgoing emails. "
    type: str

  smtp_use_tls:
    description: "Set to true to send email over a TLS connection."
    type: bool

  smtp_user:
    description: "User for SMTP authentication"
    type: str

  syslog_host:
    description: "The syslog server's IP address, for sending event logs to a syslog server."
    type: str

  syslog_ipmi_audit:
    description: "CNode and DNode IPMI commands"
    type: bool

  syslog_port:
    description: "The port number used by the syslog server to listen on for syslog requests."
    type: int

  syslog_protocol:
    description: "The protocol used for communicating with the remote syslog server."
    type: str
    choices: ["tcp", "udp"]

  syslog_shell_audit:
    description: "CNode and DNode shell commands"
    type: bool

  syslog_vms_audit:
    description: "VMS audit"
    type: bool
  state:
    description: Desired state of the resource (update-only; absent is not supported).
    type: str
    choices: [present]
    default: present
  wait:
    description: Wait for async operations to complete.
    type: bool
    default: true
  wait_timeout:
    description: Timeout in seconds for async operations.
    type: int
    default: 300
author:
  - VAST Data (@vastdata)
extends_documentation_fragment:
  - ansible.builtin.action_common_attributes
attributes:
  check_mode:
    support: full
  diff_mode:
    support: full
  platform:
    platforms: posix
"""
# END AUTOGENERATED DOCUMENTATION

# BEGIN AUTOGENERATED EXAMPLES
EXAMPLES = r"""
- name: Update EventDefinitionConfig
  vastdata.vms.eventdefinitionconfigs:
    vms:
      host: vast-vms.example.com
      token: "{{ vast_token }}"
    id: "example-value"
    state: present
"""
# END AUTOGENERATED EXAMPLES

# BEGIN AUTOGENERATED RETURN
RETURN = r"""
changed:
  description: Whether a change was made.
  type: bool
  returned: always
eventdefinitionconfigs:
  description: The eventdefinitionconfigs resource data.
  type: dict
  returned: always
  sample:
    id: 1
diff:
  description: Before/after diff when changed.
  type: dict
  returned: when changed
"""
# END AUTOGENERATED RETURN

# BEGIN AUTOGENERATED IMPORTS
from typing import Any, Dict

from ansible.module_utils.basic import AnsibleModule
from ansible_collections.vastdata.vms.plugins.module_utils.vast.errors import VastError
from ansible_collections.vastdata.vms.plugins.module_utils.vast.resource import BaseResource

# END AUTOGENERATED IMPORTS

# BEGIN AUTOGENERATED ARGUMENT_SPEC
ARGUMENT_SPEC: Dict[str, Any] = {
    "vms": {
        "type": "dict",
        "required": True,
        "options": {
            "host": {"required": True, "type": "str"},
            "validate_certs": {"type": "bool", "default": True},
            "timeout": {"type": "int", "default": None},
            "token": {"type": "str", "default": None, "no_log": True},
            "username": {"type": "str", "default": None},
            "password": {"type": "str", "default": None, "no_log": True},
            "tenant": {"type": "str", "default": None},
            "api_version": {"type": "str", "default": None},
            "debug": {"type": "bool", "default": False},
        },
        "mutually_exclusive": [
            ("token", "username"),
            ("token", "password"),
        ],
    },
    "id": {"type": "int", "default": None},
    "audit_logs_retention": {"type": "int", "default": None},
    "critical_value": {"type": "str", "default": None, "choices": ["INFO", "MINOR", "MAJOR", "CRITICAL"]},
    "disable_actions": {"type": "bool", "default": None},
    "email_recipients": {"type": "list", "default": None, "elements": "str"},
    "email_sender": {"type": "str", "default": None},
    "email_subject": {"type": "str", "default": None},
    "enabled": {"type": "bool", "default": None},
    "info_value": {"type": "str", "default": None, "choices": ["INFO", "MINOR", "MAJOR", "CRITICAL"]},
    "major_value": {"type": "str", "default": None, "choices": ["INFO", "MINOR", "MAJOR", "CRITICAL"]},
    "minor_value": {"type": "str", "default": None, "choices": ["INFO", "MINOR", "MAJOR", "CRITICAL"]},
    "quota_email_hourly_limit": {"type": "int", "default": None},
    "quota_email_interval": {"type": "str", "default": None},
    "quota_email_provider": {"type": "str", "default": None, "choices": ["aggregated", "ldap", "ad"]},
    "quota_email_suffix": {"type": "str", "default": None},
    "smtp_host": {"type": "str", "default": None},
    "smtp_password": {"type": "str", "default": None, "no_log": True},
    "smtp_port": {"type": "str", "default": None},
    "smtp_use_tls": {"type": "bool", "default": None},
    "smtp_user": {"type": "str", "default": None},
    "syslog_host": {"type": "str", "default": None},
    "syslog_ipmi_audit": {"type": "bool", "default": None},
    "syslog_port": {"type": "int", "default": None},
    "syslog_protocol": {"type": "str", "default": None, "choices": ["tcp", "udp"]},
    "syslog_shell_audit": {"type": "bool", "default": None},
    "syslog_vms_audit": {"type": "bool", "default": None},
    "state": {"type": "str", "choices": ["present"], "default": "present"},
    "wait": {"type": "bool", "default": True},
    "wait_timeout": {"type": "int", "default": 300},
}
# END AUTOGENERATED ARGUMENT_SPEC

# BEGIN AUTOGENERATED RESOURCE_CLASS


class EventdefinitionconfigResource(BaseResource):
    """Resource manager for EventDefinitionConfig."""

    resource_name = "eventdefinitionconfigs"
    singular = "eventdefinitionconfig"
    lookup_field = "id"
    update_only_fields = {
        "audit_logs_retention",
        "critical_value",
        "disable_actions",
        "email_recipients",
        "email_sender",
        "email_subject",
        "enabled",
        "info_value",
        "major_value",
        "minor_value",
        "quota_email_hourly_limit",
        "quota_email_interval",
        "quota_email_provider",
        "quota_email_suffix",
        "smtp_host",
        "smtp_password",
        "smtp_port",
        "smtp_use_tls",
        "smtp_user",
        "syslog_host",
        "syslog_ipmi_audit",
        "syslog_port",
        "syslog_protocol",
        "syslog_shell_audit",
        "syslog_vms_audit",
    }  # Fields only valid during updates


# END AUTOGENERATED RESOURCE_CLASS

# BEGIN AUTOGENERATED MAIN


def main() -> None:
    """Module entry point."""
    module = AnsibleModule(
        argument_spec=ARGUMENT_SPEC,
        supports_check_mode=True,
    )

    try:
        resource = EventdefinitionconfigResource(module)
        resource.run()
    except VastError as e:
        module.fail_json(msg=str(e))


if __name__ == "__main__":
    main()
# END AUTOGENERATED MAIN
