#!/usr/bin/python
# -*- coding: utf-8 -*-
# Copyright: (c) 2026, VAST Data
# Apache License 2.0 (see LICENSE or https://www.apache.org/licenses/LICENSE-2.0)
# SPDX-License-Identifier: Apache-2.0

from __future__ import absolute_import, division, print_function

__metaclass__ = type

# BEGIN AUTOGENERATED DOCUMENTATION
DOCUMENTATION = r"""
---
module: views_info
short_description: List VAST View resources
description:
  - List VAST View resources.
  - Read-only; always runs in check_mode and never reports changed.
  - Optional parameters are passed to the API as server-side query filters.
version_added: "1.0.0"
options:
  abe_max_depth:
    description: |
      Restricts ABE to a specified path depth. For example, if max depth is 3, ABE does not affect paths deeper than
      three levels. If not specified, ABE affects all path depths. (optional filter)
    type: int

  alias:
    description: "Filter by NFS export alias (optional filter)"
    type: str

  allow_anonymous_access:
    description: "Allow S3 anonymous access (optional filter)"
    type: bool

  allow_s3_anonymous_access:
    description: "Allow S3 anonymous access (optional filter)"
    type: bool

  allowed_delegations:
    description: |
      Which types of NFSv4 file delegations are enabled for this view. - `NONE` means NFSv4 file delegations are
      disabled. - `READ` means read type NFSv4 file delegations can be granted to a client opening a file. - `WRITE`
      means write type NFSv4 file delegations can be granted to a client opening a file. - `READ_WRITE` means both read
      and write type NFSv4 file delegations can be granted to a client opening a file. - `USE_TENANT_ALLOWED_DELEG`
      (default) means the view inherits the tenant's `allowed_delegations`. **Important:** If the tenant has `DISABLED`
      delegations, this overrides the view entirely. The effective delegations value for this view is forced to `NONE`,
      regardless of the view's setting. (optional filter)
    type: str
    choices: ["NONE", "READ", "WRITE", "READ_WRITE", "USE_TENANT_ALLOWED_DELEG"]

  auto_commit:
    description: |
      Applicable if locking is enabled. Sets the auto-commit time for files that are locked automatically. These files
      are locked automatically after the auto-commit period elapses from the time the file is saved. Files locked
      automatically are locked for the default-retention-period, after which they are unlocked. Specify as an integer
      value followed by a letter for the unit (h - hours, d - days, y - years). Example: 2h (2 hours). (optional
      filter)
    type: str

  bucket:
    description: "Limit response by S3 bucket name (optional filter)"
    type: str

  bucket_owner:
    description: "S3 Bucket owner (optional filter)"
    type: str

  bucket_owner_type:
    description: "Filter by bucket owner type (optional filter)"
    type: str

  bulk_permission_update_progress:
    description: "Progress (optional filter)"
    type: int

  bulk_permission_update_state:
    description: "State (optional filter)"
    type: str

  cluster:
    description: "Parent Cluster (optional filter)"
    type: str

  cluster__id:
    description: "Limit response by cluster ID (optional filter)"
    type: str

  cluster__name:
    description: "Filter response by cluster name. (optional filter)"
    type: str

  cluster_id:
    description: "Parent Cluster ID (optional filter)"
    type: int

  create_dir:
    description: "Creates the directory specified by the path (optional filter)"
    type: bool

  created:
    description: "Filter by created (optional filter)"
    type: str

  default_retention_period:
    description: |
      Default retention period for objects in the bucket. Required if s3_locks_retention_mode is set to governance or
      compliance. Object versions that are placed in the bucket are automatically protected with the specified
      retention for the specified amount of time. Otherwise, by default, each object version has no automatic
      protection but can be configured with a retention period or legal hold. Specify as an integer followed by h for
      hours, d for days, m for months, or y for years. For example: 2d or 1y. (optional filter)
    type: str

  directory:
    description: "Create the directory if it does not exist (optional filter)"
    type: bool

  effective_allowed_delegations:
    description: |
      The resolved NFSv4 delegation type that applies to the View. Resolution rules: - If the tenant's allowed
      delegations is `DISABLED`, the effective value is forced to `NONE`. - Otherwise, if `allowed_delegations` is
      `USE_TENANT_ALLOWED_DELEG`, the value is inherited from the Tenant's `allowed_delegations`. - In all other cases,
      the effective value equals this object's own `allowed_delegations`. This field shows the final delegation
      behavior observed by clients. (optional filter)
    type: str
    choices: ["NONE", "READ", "WRITE", "READ_WRITE"]

  files_retention_mode:
    description: |
      Applicable if locking is enabled. The retention mode for new files. For views enabled for NFSv3 or SMB, if
      locking is enabled, files_retention_mode must be set to GOVERNANCE or COMPLIANCE. If the view is enabled for S3
      and not for NFSv3 or SMB, files_retention_mode can be set to NONE. If GOVERNANCE, locked files cannot be deleted
      or changed. The Retention settings can be shortened or extended by users with sufficient permissions. If
      COMPLIANCE, locked files cannot be deleted or changed. Retention settings can be extended, but not shortened, by
      users with sufficient permissions. If NONE (S3 only), the retention mode is not set for the view; it is set
      individually for each object. (optional filter)
    type: str
    choices: ["NONE", "GOVERNANCE", "COMPLIANCE"]

  guid:
    description: "Filter by guid (optional filter)"
    type: str

  has_bucket_logging_destination:
    description: "Has a destination bucket configured as a destination for S3 bucket logging (optional filter)"
    type: bool

  has_bucket_logging_sources:
    description: |
      Is referenced by other S3 bucket views as the destination bucket for S3 bucket logging. (optional filter)
    type: bool

  has_nfs4_triggers:
    description: "Whether NFSv4 triggers are currently enabled for this view (optional filter)"
    type: bool

  id:
    description: "Filter by id (optional filter)"
    type: int

  ignore_oos:
    description: "Filter by ignore oos (optional filter)"
    type: bool

  indestructible_object_duration:
    description: "Retention period for indestructible object mode, in days. (optional filter)"
    type: int

  internal:
    description: "Filter by internal (optional filter)"
    type: bool

  is_default_subsystem:
    description: "Filter by whether View is a default Subsystem. (optional filter)"
    type: bool

  is_indestructible_object_enabled:
    description: "True if indestructible object mode is enabled. (optional filter)"
    type: bool

  is_kafka_encrypted_conn_allowed:
    description: "True if encrypted connection is allowed for Kafka (optional filter)"
    type: bool

  is_kafka_unencrypted_conn_allowed:
    description: "True if unencrypted connection is allowed for Kafka (optional filter)"
    type: bool

  is_remote:
    description: "Filter by is remote (optional filter)"
    type: bool

  is_seamless:
    description: |
      Supports seamless failover between replication peers by syncing file handles between the view and remote views on
      the replicated path on replication peers. This enables NFSv3 client users to retain the same mount point to the
      view in the event of a failover of the view path to a replication peer. This feature enables NFSv3 client users
      to retain the same mount point to the view in the event of a failover of the view path to a replication peer.
      Enabling this option may cause overhead and should only be enabled when the use case is relevant. To complete the
      configuration for seamless failover between any two peers, a seamless view must be created on each peer.
      (optional filter)
    type: bool

  kafka_encrypted_auth_mechanism:
    description: "Authentication mechanism for encrypted connection (optional filter)"
    type: str
    choices: ["NONE", "SASL_PLAIN"]

  kafka_first_join_group_timeout_sec:
    description: "Kafka first join group timeout in seconds (optional filter)"
    type: int

  kafka_is_authorization_required:
    description: "True if authorization is required for Kafka (optional filter)"
    type: bool

  kafka_rejoin_group_timeout_sec:
    description: "Kafka rejoin group timeout in seconds (optional filter)"
    type: int

  kafka_unencrypted_auth_mechanism:
    description: "Authentication mechanism for unencrypted connection (optional filter)"
    type: str
    choices: ["NONE", "SASL_PLAIN"]

  locking:
    description: "Write Once Read Many (WORM) locking enabled (optional filter)"
    type: bool

  logical_capacity:
    description: "Logical Capacity consumed by view (optional filter)"
    type: int

  max_retention_period:
    description: |
      Applicable if locking is enabled. Sets a maximum retention period for files that are locked in the view. Files
      cannot be locked for longer than this period, whether they are locked manually (by setting the atime) or
      automatically, using auto-commit. Specify as an integer value followed by a letter for the unit (m - minutes, h -
      hours, d - days, y - years). Example: 2y (2 years). (optional filter)
    type: str

  min_retention_period:
    description: |
      Applicable if locking is enabled. Sets a minimum retention period for files that are locked in the view. Files
      cannot be locked for less than this period, whether locked manually (by setting the atime) or automatically,
      using auto-commit. Specify as an integer value followed by a letter for the unit (h - hours, d - days, m -
      months, y - years). Example: 1d (1 day). (optional filter)
    type: str

  name:
    description: "Filter by View name (optional filter)"
    type: str

  nfs_interop_flags:
    description: |
      Indicates whether the view should support simultaneous access to NFS3/NFS4/SMB protocols. (optional filter)
    type: str
    choices:
      - BOTH_NFS3_AND_NFS4_INTEROP_DISABLED
      - ONLY_NFS3_INTEROP_ENABLED
      - ONLY_NFS4_INTEROP_ENABLED
      - BOTH_NFS3_AND_NFS4_INTEROP_ENABLED

  nqn:
    description: "NVMe Qualified Name to filter by. (optional filter)"
    type: str

  path:
    description: "Filter by Element Store path (optional filter)"
    type: str

  physical_capacity:
    description: "Physical Capacity consumed by view (optional filter)"
    type: int

  policy:
    description: "The name of the associated view policy (optional filter)"
    type: str

  policy__id:
    description: "Filter by view policy ID (optional filter)"
    type: str

  policy__name:
    description: "Filter by view policy name (optional filter)"
    type: str

  policy_id:
    description: "The ID of the associated view policy (optional filter)"
    type: int

  qos_policy:
    description: "QoS Policy (optional filter)"
    type: str

  qos_policy_id:
    description: "QoS Policy ID (optional filter)"
    type: int

  s3_locks_retention_mode:
    description: |
      The retention mode for new object versions stored in this bucket. You can override this if you upload a new
      object version with an explicit retention mode and period. (optional filter)
    type: str

  s3_object_ownership_rule:
    description: "Filter by s3 object ownership rule (optional filter)"
    type: str

  s3_unverified_lookup:
    description: "S3 Unverified Lookup (optional filter)"
    type: bool

  s3_versioning:
    description: "S3 Versioning enabled on S3 bucket. (optional filter)"
    type: bool

  select_for_live_monitoring:
    description: |
      True when the view has live monitoring enabled. Views that have live monitoring enabled are polled for metrics
      every ten seconds. Otherwise, views are polled every five minutes. (optional filter)
    type: bool

  share:
    description: "Filter by share name (optional filter)"
    type: str

  smb_encryption_state:
    description: "Defines the encryption level for SMB (optional filter)"
    type: str
    choices: ["OFF", "AVAILABLE", "DESIRED", "REQUIRED"]

  sync:
    description: "Synchronization state with leader (optional filter)"
    type: str

  sync_time:
    description: "Synchronization time with leader (optional filter)"
    type: str

  tenant_id:
    description: "Filter by tenant. Specify tenant ID. (optional filter)"
    type: int

  tenant_name:
    description: "Tenant Name (optional filter)"
    type: str

  tenant_name__icontains:
    description: "Tenant name to filter by (optional filter)"
    type: str

  title:
    description: "Filter by title (optional filter)"
    type: str

  url:
    description: "The endpoint URL for API operations on the view (optional filter)"
    type: str
author:
  - VAST Data (@vastdata)
extends_documentation_fragment:
  - vastdata.vms.conn
  - ansible.builtin.action_common_attributes
attributes:
  check_mode:
    support: full
  diff_mode:
    support: N/A
  platform:
    platforms: posix
"""
# END AUTOGENERATED DOCUMENTATION

# BEGIN AUTOGENERATED EXAMPLES
EXAMPLES = r"""
- name: List all views
  vastdata.vms.views_info:
    vms:
      host: vast-vms.example.com
      token: "{{ vast_token }}"
  register: views_result

- name: List views filtered by path
  vastdata.vms.views_info:
    vms:
      host: vast-vms.example.com
      token: "{{ vast_token }}"
    path: example-value
  register: filtered_views
"""
# END AUTOGENERATED EXAMPLES

# BEGIN AUTOGENERATED RETURN
RETURN = r"""
changed:
  description: Always false; info modules never change state.
  type: bool
  returned: always
views:
  description: List of matching views resources.
  type: list
  elements: dict
  returned: always
  sample: []
"""
# END AUTOGENERATED RETURN

# BEGIN AUTOGENERATED IMPORTS
from typing import Any, Dict

from ansible.module_utils.basic import AnsibleModule
from ansible_collections.vastdata.vms.plugins.module_utils.vast.errors import VastError
from ansible_collections.vastdata.vms.plugins.module_utils.vast.info_resource import BaseInfoResource

# END AUTOGENERATED IMPORTS

# BEGIN AUTOGENERATED ARGUMENT_SPEC
ARGUMENT_SPEC: Dict[str, Any] = {
    "vms": {
        "type": "dict",
        "required": True,
        "options": {
            "host": {"required": True, "type": "str"},
            "validate_certs": {"type": "bool", "default": True},
            "timeout": {"type": "int", "default": None},
            "token": {"type": "str", "default": None, "no_log": True},
            "username": {"type": "str", "default": None},
            "password": {"type": "str", "default": None, "no_log": True},
            "tenant": {"type": "str", "default": None},
            "api_version": {"type": "str", "default": None},
            "debug": {"type": "bool", "default": False},
        },
        "mutually_exclusive": [
            ("token", "username"),
            ("token", "password"),
        ],
    },
    "abe_max_depth": {"type": "int", "default": None},
    "alias": {"type": "str", "default": None},
    "allow_anonymous_access": {"type": "bool", "default": None},
    "allow_s3_anonymous_access": {"type": "bool", "default": None},
    "allowed_delegations": {
        "type": "str",
        "default": None,
        "choices": ["NONE", "READ", "WRITE", "READ_WRITE", "USE_TENANT_ALLOWED_DELEG"],
    },
    "auto_commit": {"type": "str", "default": None},
    "bucket": {"type": "str", "default": None},
    "bucket_owner": {"type": "str", "default": None},
    "bucket_owner_type": {"type": "str", "default": None},
    "bulk_permission_update_progress": {"type": "int", "default": None},
    "bulk_permission_update_state": {"type": "str", "default": None},
    "cluster": {"type": "str", "default": None},
    "cluster__id": {"type": "str", "default": None},
    "cluster__name": {"type": "str", "default": None},
    "cluster_id": {"type": "int", "default": None},
    "create_dir": {"type": "bool", "default": None},
    "created": {"type": "str", "default": None},
    "default_retention_period": {"type": "str", "default": None},
    "directory": {"type": "bool", "default": None},
    "effective_allowed_delegations": {"type": "str", "default": None, "choices": ["NONE", "READ", "WRITE", "READ_WRITE"]},
    "files_retention_mode": {"type": "str", "default": None, "choices": ["NONE", "GOVERNANCE", "COMPLIANCE"]},
    "guid": {"type": "str", "default": None},
    "has_bucket_logging_destination": {"type": "bool", "default": None},
    "has_bucket_logging_sources": {"type": "bool", "default": None},
    "has_nfs4_triggers": {"type": "bool", "default": None},
    "id": {"type": "int", "default": None},
    "ignore_oos": {"type": "bool", "default": None},
    "indestructible_object_duration": {"type": "int", "default": None},
    "internal": {"type": "bool", "default": None},
    "is_default_subsystem": {"type": "bool", "default": None},
    "is_indestructible_object_enabled": {"type": "bool", "default": None},
    "is_kafka_encrypted_conn_allowed": {"type": "bool", "default": None},
    "is_kafka_unencrypted_conn_allowed": {"type": "bool", "default": None},
    "is_remote": {"type": "bool", "default": None},
    "is_seamless": {"type": "bool", "default": None},
    "kafka_encrypted_auth_mechanism": {"type": "str", "default": None, "choices": ["NONE", "SASL_PLAIN"]},
    "kafka_first_join_group_timeout_sec": {"type": "int", "default": None},
    "kafka_is_authorization_required": {"type": "bool", "default": None},
    "kafka_rejoin_group_timeout_sec": {"type": "int", "default": None},
    "kafka_unencrypted_auth_mechanism": {"type": "str", "default": None, "choices": ["NONE", "SASL_PLAIN"]},
    "locking": {"type": "bool", "default": None},
    "logical_capacity": {"type": "int", "default": None},
    "max_retention_period": {"type": "str", "default": None},
    "min_retention_period": {"type": "str", "default": None},
    "name": {"type": "str", "default": None},
    "nfs_interop_flags": {
        "type": "str",
        "default": None,
        "choices": [
            "BOTH_NFS3_AND_NFS4_INTEROP_DISABLED",
            "ONLY_NFS3_INTEROP_ENABLED",
            "ONLY_NFS4_INTEROP_ENABLED",
            "BOTH_NFS3_AND_NFS4_INTEROP_ENABLED",
        ],
    },
    "nqn": {"type": "str", "default": None},
    "path": {"type": "str", "default": None},
    "physical_capacity": {"type": "int", "default": None},
    "policy": {"type": "str", "default": None},
    "policy__id": {"type": "str", "default": None},
    "policy__name": {"type": "str", "default": None},
    "policy_id": {"type": "int", "default": None},
    "qos_policy": {"type": "str", "default": None},
    "qos_policy_id": {"type": "int", "default": None},
    "s3_locks_retention_mode": {"type": "str", "default": None},
    "s3_object_ownership_rule": {"type": "str", "default": None},
    "s3_unverified_lookup": {"type": "bool", "default": None},
    "s3_versioning": {"type": "bool", "default": None},
    "select_for_live_monitoring": {"type": "bool", "default": None},
    "share": {"type": "str", "default": None},
    "smb_encryption_state": {"type": "str", "default": None, "choices": ["OFF", "AVAILABLE", "DESIRED", "REQUIRED"]},
    "sync": {"type": "str", "default": None},
    "sync_time": {"type": "str", "default": None},
    "tenant_id": {"type": "int", "default": None},
    "tenant_name": {"type": "str", "default": None},
    "tenant_name__icontains": {"type": "str", "default": None},
    "title": {"type": "str", "default": None},
    "url": {"type": "str", "default": None},
}
# END AUTOGENERATED ARGUMENT_SPEC

# BEGIN AUTOGENERATED RESOURCE_CLASS


class ViewInfoResource(BaseInfoResource):
    """Read-only list manager for View."""

    resource_name = "views"
    return_key = "views"
    filter_fields = {
        "abe_max_depth",
        "alias",
        "allow_anonymous_access",
        "allow_s3_anonymous_access",
        "allowed_delegations",
        "auto_commit",
        "bucket",
        "bucket_owner",
        "bucket_owner_type",
        "bulk_permission_update_progress",
        "bulk_permission_update_state",
        "cluster",
        "cluster__id",
        "cluster__name",
        "cluster_id",
        "create_dir",
        "created",
        "default_retention_period",
        "directory",
        "effective_allowed_delegations",
        "files_retention_mode",
        "guid",
        "has_bucket_logging_destination",
        "has_bucket_logging_sources",
        "has_nfs4_triggers",
        "id",
        "ignore_oos",
        "indestructible_object_duration",
        "internal",
        "is_default_subsystem",
        "is_indestructible_object_enabled",
        "is_kafka_encrypted_conn_allowed",
        "is_kafka_unencrypted_conn_allowed",
        "is_remote",
        "is_seamless",
        "kafka_encrypted_auth_mechanism",
        "kafka_first_join_group_timeout_sec",
        "kafka_is_authorization_required",
        "kafka_rejoin_group_timeout_sec",
        "kafka_unencrypted_auth_mechanism",
        "locking",
        "logical_capacity",
        "max_retention_period",
        "min_retention_period",
        "name",
        "nfs_interop_flags",
        "nqn",
        "path",
        "physical_capacity",
        "policy",
        "policy__id",
        "policy__name",
        "policy_id",
        "qos_policy",
        "qos_policy_id",
        "s3_locks_retention_mode",
        "s3_object_ownership_rule",
        "s3_unverified_lookup",
        "s3_versioning",
        "select_for_live_monitoring",
        "share",
        "smb_encryption_state",
        "sync",
        "sync_time",
        "tenant_id",
        "tenant_name",
        "tenant_name__icontains",
        "title",
        "url",
    }
    generated_min_version = (5, 4)  # oldest VMS version these modules were generated for
    field_versions = {
        "allowed_delegations": {"min": (5, 5), "max": None},
    }


# END AUTOGENERATED RESOURCE_CLASS

# BEGIN AUTOGENERATED MAIN


def main() -> None:
    """Module entry point."""
    module = AnsibleModule(
        argument_spec=ARGUMENT_SPEC,
        supports_check_mode=True,
    )

    try:
        resource = ViewInfoResource(module)
        resource.run()
    except VastError as e:
        module.fail_json(msg=str(e))


if __name__ == "__main__":
    main()
# END AUTOGENERATED MAIN
